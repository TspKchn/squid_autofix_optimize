# 🐙 Squid AutoFix & Optimize Script

**Version:** 1.0.0  
**Author:** ChatGPT  

---

## ✨ Overview

`squid_autofix_optimize.sh` เป็นสคริปต์อัตโนมัติสำหรับจัดการและปรับแต่ง **Squid Proxy** ให้ทำงานได้มีประสิทธิภาพสูงสุด ✅  
สคริปต์นี้จะช่วยคุณ:

- 💾 ปรับแต่ง `squid.conf` ให้เหมาะสมกับ VPS/RAM ของคุณ  
- 🧹 ลบ ACL/HTTP access ที่ไม่จำเป็น  
- 🏎️ เพิ่มประสิทธิภาพการใช้ Cache Memory  
- 📄 จัดการ Logrotate สำหรับไฟล์ log ของ Squid  
- ⏱️ ตั้ง Cron job ให้รันทุก 5 นาที  

---

## ⚙️ Features

1. 🔍 ตรวจสอบเวอร์ชัน Squid อัตโนมัติ  
2. 🛠️ Backup และ Optimize `squid.conf`  
3. 🗑️ ลบ ACL และ HTTP Access ที่ไม่จำเป็น 4 บรรทัดหลัก  
4. 🧮 ตรวจสอบการใช้ RAM และปรับค่า cache memory อัตโนมัติ  
5. 📦 สร้าง directory cache ถ้าไม่พบ  
6. 📝 ตั้ง Logrotate สำหรับ Squid log  
7. ⏰ ตั้ง Cron job ให้รันทุก 5 นาที  
8. ✅ ตรวจสอบและ reload Squid หลังปรับแต่ง  

---

## 💻 Usage

ดาวน์โหลดสคริปต์ & รันสคริปต์ด้วย root & ตรวจสอบ log

```bash
wget https://raw.githubusercontent.com/TspKchn/squid_autofix_optimize/main/squid_autofix_optimize.sh -O /usr/local/bin/squid_autofix_optimize.sh
chmod +x /usr/local/bin/squid_autofix_optimize.sh
sudo /usr/local/bin/squid_autofix_optimize.sh
tail -n 10 /var/log/squid_autofix.log

```

---

## 📂 Log & Cron

- Log: `/var/log/squid_autofix.log` 📝  
- Cron job: รันทุก 5 นาที ⏰  

```bash
*/5 * * * * /usr/local/bin/squid_autofix_optimize.sh >> /var/log/squid_autofix.log 2>&1
```

---

## ⚠️ Warnings

- 🚨 Warning ใน log เกี่ยวกับ ACL `localhost` หรือ `to_localhost` เป็นปกติ ไม่ส่งผลต่อการทำงาน  
- 🔄 สคริปต์จัดการ RAM/Swap ของ Squid เอง ไม่ต้องกังวลว่าจะชนกับ swap ของระบบ  

---

## ❤️ Support

หากมีคำถามหรือปัญหา สามารถเปิด Issue ใน Repo ของคุณได้เลย!  

---

> Made with ❤️ and 🐙 for Squid Proxy

